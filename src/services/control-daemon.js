(function(angular, app) {
  app.factory('daemon',[ 'toast', 'ajax', function(toast, ajax){
    //TODO: add logic

    // Will contain daemon control methods that call a rest endpoint
    // Will make an error toast
    // possible take a callback function to revert UI changes if an update fails
  }]);
})(window.angular, app)
